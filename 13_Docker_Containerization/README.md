# 13. Docker & Containerization

Multi-stage builds and Docker Compose orchestration for full-stack AI services.

## 📋 Checklist
- [ ] Multi-stage `Dockerfile` for FastAPI backend
- [ ] Multi-stage `Dockerfile` for React (Node build -> Nginx runtime)
- [ ] Multi-container `docker-compose.yml` (Frontend, Backend, MySQL)
- [ ] Container network configuration & volume persistence

## 📁 Starter Code
- See [Dockerfile.fastapi](Dockerfile.fastapi), [Dockerfile.react](Dockerfile.react), and [docker-compose.yml](docker-compose.yml).
